services:
  waywire:
    command: .agents/serve-desktop
    platforms: [linux]
    health: /healthz
    portal:
      title: Waywire desktop
      description: This checkout's headless Smithay compositor and browser viewer. Restart the service after source changes.
